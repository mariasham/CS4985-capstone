We use a pre-trained transformer model as the base.
Pre-trained models already understand general language patterns, which makes them ideal for fine-tuning on specific tasks like sentiment analysis.
This approach saves time and computational resources.
